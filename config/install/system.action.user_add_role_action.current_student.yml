langcode: en
status: true
dependencies:
  config:
    - user.role.current_student
  module:
    - user
id: user_add_role_action.current_student
label: 'Add the current student role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: current_student
